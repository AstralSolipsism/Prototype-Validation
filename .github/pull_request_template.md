# 验证对象

说明本 PR 试图证伪或证明的风险假设。

## 变更

说明实现、测试和文档变更。

## 证据

- [ ] 静态架构检查
- [ ] Rust 格式检查
- [ ] Clippy
- [ ] 单元/集成测试
- [ ] 无头报告
- [ ] GPU 截图或录像（视觉 PR）

## 停止线状态

说明该阶段仍缺哪些证据。除非全部满足，不得把 `prototype-status.json` 中的 `passed` 改为 `true`。

## 明确未做

列出本 PR 有意不包含的系统，防止范围悄然扩大。
