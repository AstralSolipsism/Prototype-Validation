# Integrated P4 isolated validation trigger

This branch exists only to trigger the validation workflow stored on `agent/p4-integrated-validation-runner`. The workflow checks out and validates `agent/p4-integrated-world-pipeline` directly.
