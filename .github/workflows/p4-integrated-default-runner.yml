name: p4-integrated-default-runner

on:
  pull_request:
    paths:
      - docs/evidence/p4-integrated-validation-trigger.md
  workflow_dispatch:

permissions:
  contents: read

jobs:
  smoke:
    name: confirm integrated P4 runner registration
    runs-on: ubuntu-latest
    steps:
      - name: Confirm trigger
        run: echo "Integrated P4 default-branch runner is registered."
